import { EmbedvideourlPipe } from './embedvideourl.pipe';

describe('EmbedvideourlPipe', () => {
  it('create an instance', () => {
    const pipe = new EmbedvideourlPipe();
    expect(pipe).toBeTruthy();
  });
});
